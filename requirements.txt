# Web Framework & Server
fastapi
uvicorn
pydantic

# LangGraph & LangChain Core
langgraph
langchain-core

# LLM Provider
langchain-openai
langchain-ollama

# apimaker (claude CLI 세션 래퍼) - in-process 라이브러리로 직접 사용 (별도 HTTP 서버 불필요)
-e ../../apimaker[claude]

# RDB (MariaDB 직결)
pymysql

# Shared POLARIS retrieval package
-e ../pola

# Environment Variables
python-dotenv

# Test
pytest

langchain_google_genai
pymysql